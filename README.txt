BEAST BBC — camera roll fix

Upload the files from this ZIP into your existing GitHub repository root, replacing matching files. Keep all other existing files, images and CNAME. This is an update package, not a complete replacement repository.

The camera roll now displays full images at their natural aspect ratio. A single photo has a centered frame up to 760px wide; additional photos retain the newest-first horizontal strip. Add future camera-roll-item figures before the current figure in index.html.

index.html and script.js are preserved exactly from the latest crew-header-restored version. Only camera-roll CSS changed. The restored crew photo, first camera-roll photo and supplied pseudo-live segment (latest-live.mp4) are included.
